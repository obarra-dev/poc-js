test("adds 1 + 2 to equal 3", () => {
  expect(3).toBe(3);
});

describe("groped tests", () => {
  it("test one", () => {
    expect(8.0).toBe(8.0);
  });

  it("test two", () => {
    expect(5.0).toBe(5.0);
  });
});
